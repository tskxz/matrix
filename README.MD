# Matrix Project

## How to use
- ```git clone/pull``` the project
- run ```env\Scripts\activate```, if you are on linux use ```source env/bin/activate```
- install packages using ```pip install requirements.txt```
- then proceed to run ```python app.py```